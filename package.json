{
  "name": "my-electron-app",
  "version": "1.0.0",
  "description": "Hello World!",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "prebuild": "cross-env-shell \"if not exist dist mkdir dist\"",
    "build": "cross-env-shell \"copy index.html dist && copy styles.css dist && copy main.js dist\"",
    "vercel-build": "npm run prebuild && npm run build"
  },
  "author": "Mah",
  "license": "MIT",
  "devDependencies": {
    "cross-env": "^7.0.3",
    "electron": "23.1.3"
  }
}
