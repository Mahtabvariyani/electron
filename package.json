{
  "name": "my-electron-app",
  "version": "1.0.0",
  "description": "Hello World!",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build": "if not exist dist mkdir dist && if exist index.html copy index.html dist && if exist styles.css copy styles.css dist && if exist main.js copy main.js dist",
    "vercel-build": "npm run build"
  },
  "author": "Mah",
  "license": "MIT",
  "devDependencies": {
    "electron": "23.1.3"
  }
}
